{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "sh -c 'uvicorn bot_server:app --host 0.0.0.0 --port ${PORT:-8080}'",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
